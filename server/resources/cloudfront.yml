Resources:
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Origins:
          - Id: AppOrigin
            DomainName: !GetAtt AppBucket.RegionalDomainName # for website origin we need the regional domain name # <bucket-name>.s3.<region>.amazonaws.com
            CustomOriginConfig:
              HTTPPort: 80
              HTTPSPort: 443
              OriginProtocolPolicy: http-only # Do I need https-only?
        DefaultCacheBehavior:
          TargetOriginId: AppOrigin
          ForwardedValues:
            QueryString: true
          ViewerProtocolPolicy: redirect-to-https
          # MinTTL: 0
        DefaultRootObject: index.html
        CustomErrorResponses:
          - ErrorCode: 404
            ResponseCode: 200
            ResponsePagePath: /index.html
        Aliases: !If
          - HasCloudFrontDnsAlias
          - - ${self:custom.domainName}
          - !Ref AWS::NoValue
        ViewerCertificate: !If
          - HasCloudFrontDnsAlias
          - AcmCertificateArn: ${self:custom.certificateArn}
            SslSupportMethod: sni-only
            # MinimumProtocolVersion: _ # I didn't set this, it defaulted to TLSv1
          - CloudFrontDefaultCertificate: true
        Enabled: true
